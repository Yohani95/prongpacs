package org.prongpa.Repository.Config;

import org.prongpa.Models.ConfigDBModel;
import org.prongpa.Models.TaskModel;
import org.prongpa.Repository.GenericRepository;

public interface ConfigRepository extends GenericRepository<ConfigDBModel, Long> {
}
